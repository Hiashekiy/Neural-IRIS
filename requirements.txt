# Neural-IRIS minimal Python dependencies
#
# Notes:
# - `torch` / `torchvision` can be installed from the official PyTorch index
#   if you need a CUDA-enabled build.
# - `hpipm_python` is used by the MPC planner path, but it is not listed here
#   because it is typically built/installed separately rather than pulled from PyPI.
# - If you only need training / evaluation / plotting, you do not need `hpipm_python`.

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
tqdm>=4.66
opencv-python>=4.8
torch>=2.4
torchvision>=0.19
cvxpy>=1.4
